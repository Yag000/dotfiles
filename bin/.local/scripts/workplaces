#!/usr/bin/bash


workplaces_dir=~/dotfiles/ressources/workplaces.txt

if [[ $# -eq 1 ]]; then
 find ~/Documents/l2_math-info/s4 ~/Projects ~/dotfiles -maxdepth 3 -name .git -exec dirname {} \; -prune > $workplaces_dir
else
    cat $workplaces_dir
fi

